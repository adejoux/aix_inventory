.offset1
  = simple_form_for @report do |f|
    = f.input :name
    = f.input :description
    = f.hidden_field :selected_fields, value: "tbd"
    = f.fields_for :report_fields do |builder|
      %fieldset
        = builder.label :content, "Server Field"
        = builder.collection_select :select_attribute, ServerAttribute.group(:name), :name, :name
        = link_to "remove", '#', class: "remove_fields btn btn-danger"
    %br
    = link_to_add_report_fields "Add Conditions", f, :report_fields
    %br
    %br
    = link_to "back", reports_path, class: "btn btn-warning"
    = f.submit
