%h1 Select report scope
.row-fluid
  .span4
    %h2.ui-widget-header os type
    .ui-widget-content
      %ul
        - OperatingSystemType.all.each do |os_type|
          %li
            = check_box_tag "report[operating_system_type_ids][]", os_type.id, report.operating_system_type_ids.include?(os_type.id)
            = os_type.name
  .span8
    %h2.ui-widget-header customers
    #customers.ui-widget-content
      #check_all.btn Check / Uncheck All
      %ul.inline
        - Customer.all.each do |customer|
          %li
            = check_box_tag "report[customer_ids][]", customer.id, report.customer_ids.include?(customer.id)
            = customer.name
